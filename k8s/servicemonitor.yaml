apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: app-migration-monitor
  namespace: default
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: app-migration
  endpoints:
    - port: http
      path: /metrics
      interval: 15s